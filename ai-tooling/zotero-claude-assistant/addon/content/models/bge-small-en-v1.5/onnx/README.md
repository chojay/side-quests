# bge-small-en-v1.5 ONNX weights

The quantized model weights (`model_quantized.onnx`, ~32 MB) are not committed to git.
Fetch them before building:

```bash
./scripts/fetch-model.sh
```

Source: https://huggingface.co/Xenova/bge-small-en-v1.5
